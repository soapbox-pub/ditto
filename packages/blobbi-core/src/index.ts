/**
 * @blobbi/core — portable, framework-agnostic core domain logic for Blobbi.
 *
 * Phase 1: scaffold only. No Blobbi logic has been moved here yet; the package
 * is consumed from source via aliases (`@blobbi/core`, `@blobbi/core/*`).
 */

export { blobbiLogger, setBlobbiLogger, type BlobbiLogger } from './logger.ts';
